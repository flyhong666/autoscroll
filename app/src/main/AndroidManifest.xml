<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 振动（悬浮窗长按反馈） -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 屏幕常亮（WakeLock） -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 查询其他应用（包名检测、轮换启动） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <!-- 精确闹钟（定时运行） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 通知权限（Android 13+ 前台服务通知可见性） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 读取网络状态（判断是否处于 WiFi） -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoScroll">

        <!-- 主界面 -->
        <activity
            android:name="cn.ggdoc.autoscroll.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 无障碍服务 -->
        <service
            android:name="cn.ggdoc.autoscroll.service.AutoScrollAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 悬浮窗前台服务 -->
        <service
            android:name="cn.ggdoc.autoscroll.service.FloatingWindowService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="悬浮窗控制自动滚动" />
        </service>

        <!-- 录制 / 回放悬浮控制条 -->
        <service
            android:name="cn.ggdoc.autoscroll.service.RecorderOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="悬浮控制条录制/回放操作脚本" />
        </service>

        <!-- 定时运行闹钟广播接收器 -->
        <receiver
            android:name="cn.ggdoc.autoscroll.service.ScheduleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.ggdoc.autoscroll.SCHEDULE_START" />
                <action android:name="cn.ggdoc.autoscroll.SCHEDULE_END" />
            </intent-filter>
        </receiver>

        <!-- 运行日志页（崩溃监控 / 日志查看 / 统计导出） -->
        <activity
            android:name="cn.ggdoc.autoscroll.ui.LogActivity"
            android:exported="false"
            android:label="@string/log_title" />

        <!-- 健康诊断页 -->
        <activity
            android:name="cn.ggdoc.autoscroll.ui.DiagnosticsActivity"
            android:exported="false"
            android:label="@string/diagnostics_title" />

        <!-- 应用中心 -->
        <activity
            android:name="cn.ggdoc.autoscroll.ui.AppCenterActivity"
            android:exported="false"
            android:label="@string/app_center_title" />

        <!-- 桌面小部件（清单 #11） -->
        <receiver
            android:name="cn.ggdoc.autoscroll.widget.AutoScrollWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="cn.ggdoc.autoscroll.WIDGET_TOGGLE" />
                <action android:name="cn.ggdoc.autoscroll.STATE_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_autoscroll_info" />
        </receiver>

    </application>
</manifest>
